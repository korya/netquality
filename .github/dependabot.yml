version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    # One PR for all action bumps instead of one per action.
    groups:
      actions:
        patterns:
          - "*"
    commit-message:
      prefix: ci
